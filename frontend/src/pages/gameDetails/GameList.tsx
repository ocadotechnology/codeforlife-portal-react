import React from 'react';
import {
  Unstable_Grid2 as Grid
} from '@mui/material';

const GameList: React.FC = () => {
  return (
    <Grid xs={12}>
      GameList
    </Grid>
  );
};

export default GameList;
