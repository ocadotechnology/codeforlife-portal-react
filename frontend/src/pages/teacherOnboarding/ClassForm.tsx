import React from 'react';
import {
  Stack
} from '@mui/material';

const ClassForm: React.FC<{
  nextStep: () => void
}> = ({
  nextStep
}) => {
    return (
      <Stack>

      </Stack>
    );
  };

export default ClassForm;
