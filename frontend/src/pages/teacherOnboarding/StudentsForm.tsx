import React from 'react';
import {
  Stack
} from '@mui/material';

const StudentsForm: React.FC<{
  nextStep: () => void
}> = ({
  nextStep
}) => {
    return (
      <Stack>

      </Stack>
    );
  };

export default StudentsForm;
