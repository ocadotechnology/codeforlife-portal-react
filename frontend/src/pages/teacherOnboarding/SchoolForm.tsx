import React from 'react';
import {
  Stack
} from '@mui/material';

const SchoolForm: React.FC<{
  nextStep: () => void
}> = ({
  nextStep
}) => {
    return (
      <Stack>

      </Stack>
    );
  };

export default SchoolForm;
